// Create a structure type ‘book’ with name, price and number of pages as its attributes

#include <stdio.h>
int main(){
    struct book{
        char name;
        float price;
        int numberOfPages;
    };

}