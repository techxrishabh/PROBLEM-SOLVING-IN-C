// ================== puts() ================================


#include <stdio.h>
#include <string.h>
int main(){
    char str[] = "College wallah is best";
    printf("%s\n", str);
    puts("Hello Everyone"); // we can also write that as 
    return 0;

}